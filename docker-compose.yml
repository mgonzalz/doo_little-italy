version: '3.9'

services:
  django:
    image: pizzeria:0.0.1
    build: .
    ports:
      - "8080:8080"  # Mapeo del puerto, no modificar
    env_file:
      - pizzeria/.env  # Cargar variables de entorno desde el archivo .env
    volumes:
      - .:/app  # Mapear el código local al contenedor para desarrollo
    command: ["python", "pizzeria/manage.py", "runserver", "0.0.0.0:8080"]
